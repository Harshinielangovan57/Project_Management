# Server Configuration
PORT=5000
NODE_ENV=development
CLIENT_URL=http://localhost:5173

# PostgreSQL Database Configuration
DB_DIALECT=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=project_management
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false

# Optional PostgreSQL Connection URL (e.g. Neon, Supabase, Render, Railway, AWS RDS)
# DATABASE_URL=postgresql://postgres:password@localhost:5432/project_management

# JWT Security Configuration
JWT_SECRET=super_secret_jwt_project_management_token_key_2026_secure
JWT_EXPIRES_IN=7d
